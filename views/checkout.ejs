<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
  </head>
  <body>
    <div class="container">
      <header>
        <h1><i class="fas fa-sign-out-alt"></i> Check-out Karyawan</h1>
        <p class="subtitle">Update jam pulang karyawan yang sudah check-in</p>
      </header>

      <!-- User Info -->
      <% if (user) { %>
      <div class="user-info">
        <div class="user-details">
          <i class="fas fa-user-circle"></i>
          <strong><%= user.username %></strong>
          <span class="role-user">MITRA</span>
        </div>
        <a href="/logout" class="logout-btn">
          <i class="fas fa-sign-out-alt"></i> Logout
        </a>
      </div>
      <% } %>

      <div class="table-container">
        <% if (data.length === 0) { %>
        <div class="empty-state">
          <i class="fas fa-check-circle"></i>
          <h3>Semua karyawan sudah check-out!</h3>
          <p>Tidak ada data yang perlu di-check-out.</p>
        </div>
        <% } else { %>
        <table>
          <thead>
            <tr>
              <th>ID</th>
              <th>Nama</th>
              <th>Tanggal</th>
              <th>Jam Masuk</th>
              <th>Aksi</th>
            </tr>
          </thead>
          <tbody>
            <% data.forEach(item => { %>
            <tr>
              <td><%= item.id %></td>
              <td><strong><%= item.nama %></strong></td>
              <td>
                <% const tgl = new Date(item.tanggal); const formatted =
                tgl.toLocaleDateString('id-ID'); %> <%= formatted %>
              </td>
              <td><%= item.jam_masuk %></td>
              <td>
                <!-- Form untuk langsung check-out -->
                <form
                  action="/checkout/<%= item.id %>"
                  method="POST"
                  style="display: inline"
                >
                  <button type="submit" class="btn-action btn-checkout">
                    <i class="fas fa-sign-out-alt"></i> Check-out
                  </button>
                </form>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
        <% } %>
      </div>

      <div class="action-buttons">
        <a href="/" class="btn btn-back">
          <i class="fas fa-arrow-left"></i> Kembali ke Beranda
        </a>
        <!-- TIDAK ADA TOMBOL LIHAT LAPORAN -->
      </div>
    </div>
  </body>
</html>
